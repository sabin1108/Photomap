# 2026-08-08 사용자 1만 명 부하 준비 결과

기준 시각: `2026-08-08 20:31:56 KST`

## 완료

- GitHub #18에서 10,000 MAU traffic model과 staging 경계를 정의했다.
- GitHub #19에서 Supabase 초기 read 3개를 재현하는 k6 scenario를 추가했다.
- `smoke`, `normal`, `peak`, `spike`, `soak` profile과 threshold를 코드화했다.
- GitHub #20에서 사진 관계 조회의 `select *`를 제거했다.
- 추가 사진 page가 favorites 전체를 다시 조회하지 않고 session cache를 재사용한다.
- 추가 page의 Supabase 요청 수는 2개에서 1개로 감소했다.

## 로컬 검증

```text
npm run load:check                         PASS
npm run test:load-contract                 PASS, 6 tests
npx tsc --noEmit                           PASS
npm run build:perf                         PASS
npx playwright test tests/unity-map-performance.spec.js
                                            PASS, 7 tests
```

## 아직 실행하지 않은 검증

staging Supabase URL, test user, anon key와 production 유사 dataset이 이 작업 공간에
없다. 설치된 k6 binary도 없다. 따라서 외부 서비스에 임의 트래픽을 보내지 않았고
p95, p99, 429, DB CPU, memory, connection, egress 실측값을 만들지 않았다.

승인 없는 Vercel production 부하 테스트도 실행하지 않았다. GitHub #21은 staging
환경에서 다음 검증을 마칠 때까지 열어 둔다.

- peak: API 약 24 RPS, 30분.
- spike: API 최대 약 60 RPS, 약 6분.
- soak: API 약 15 RPS, 2시간.

## 다음 개선점

1. staging에 production과 같은 RLS와 index를 적용하고 합성 metadata를 적재한다.
2. `(user_id, created_time desc)`, favorites와 관계 FK의 query plan을 확인한다.
3. offset pagination을 `(created_time, media_id)` cursor pagination으로 바꾼다.
4. test 결과의 response byte와 Supabase resource metric을 함께 보존한다.
5. p95 300ms 또는 p99 800ms 기준 실패 endpoint부터 index와 response shape를 조정한다.

원본 환경 문서:

```text
E:\memory\photomap\2026-08-08-10k-user-load-test-environment.md
```

## 2026-08-08 추가 실측

### Vercel 공개 URL

대상은 `https://photomap-three.vercel.app/`이며 현재 HEAD의 Vercel 배포 성공을
확인했다. Lighthouse 기본 mobile throttling을 순차 3회 실행했다.

| 지표 | 2026-07-30 중앙값 | 2026-08-08 중앙값 | 변화 |
| --- | ---: | ---: | ---: |
| Performance | 89 | 87 | -2점 |
| FCP | 1447ms | 1452ms | 0.3% 저하 |
| LCP | 3739ms | 3748ms | 0.2% 저하 |
| TBT | 35ms | 56ms | 21ms 증가 |
| CLS | 0.000177 | 0.000177 | 동일 |
| Speed Index | 2336ms | 2202ms | 5.7% 개선 |
| TTI | 3777ms | 3755ms | 0.6% 개선 |
| Payload | 347947B | 347619B | 0.1% 감소 |

LCP discovery는 통과했고 document server response는 약 6ms다. 중앙값 run의
LCP element render delay는 약 810ms, Lighthouse 추정 미사용 JavaScript는
65,835B다. LCP 3748ms는 2500ms quality gate를 통과하지 못한다.

### localhost production preview

HTML, favicon, entry JS, entry CSS, Unity map HTML 5개를 한 iteration으로 묶어
각 15초 동안 closed concurrency를 측정했다.

| 동시 worker | Request/s | p95 | p99 | 오류율 |
| ---: | ---: | ---: | ---: | ---: |
| 10 | 1073.03 | 51.78ms | 66.61ms | 0% |
| 50 | 1058.86 | 259.36ms | 328.21ms | 0% |
| 100 | 1078.72 | 510.13ms | 656.45ms | 0% |
| 200 | 1019.50 | 1083.13ms | 1516.27ms | 0% |

약 1050 request/s에서 처리량이 포화되고 concurrency 증가가 tail latency를
키웠다. 같은 PC의 Vite preview 결과이므로 Vercel CDN 또는 Supabase capacity
증거로 사용하지 않는다.

### 기능·보안

- 공개 지도 흐름 1개와 Unity 성능 계약 7개 통과.
- load runner 안전 계약 9개 통과.
- production dependency 107개 기준 npm audit 취약점 0개.
- HSTS와 Vercel cache HIT 확인.
- CSP, nosniff, Referrer-Policy, Permissions-Policy는 공개 HTML response에서
  확인되지 않아 보완 후보로 기록.

상세 보고서:

```text
E:\memory\photomap\characteristic\2026-08-08-load-vercel-security-report.md
```

## 지표 유효성 판정

- Lighthouse LCP 3748ms는 실제 주요 사진이 보이는 lab 지표라 체감 신호로
  유의미하다. 다만 실제 사용자 p75가 아니므로 field 값으로 소개하지 않는다.
- 세 run 모두 2500ms를 넘었다. 현재 첫 화면은 일관되게 개선 필요 구간이다.
- 7월 30일 대비 LCP +9ms는 측정 오차다. 개선이나 회귀가 아니라 정체로 판단한다.
- TBT 56ms는 mobile lab 기준 fast다. +60%보다 절대 증가 +21ms가 중요하며 실제
  INP가 좋다는 근거는 아니다.
- CLS 0.000177은 측정한 초기 load에서 유의미하게 안정적이다. 실제 사용자 p75와
  지도·modal interaction 중 CLS는 별도로 수집해야 한다.
- localhost 약 1050 request/s는 사용자 체감이나 실서비스 capacity 지표가 아니다.
  Vite preview와 load runner의 local 포화 특성만 설명한다.
- Playwright 통과는 기능 안정성, npm audit 0은 알려진 dependency advisory 부재만
  뜻한다.
- 현재 자료로 사용자 10,000명 수용 가능 주장은 하지 않는다.

실사용 판단에는 mobile/desktop별 field p75 LCP, INP, CLS와 사진 data ready,
map ready, photo open latency, error rate가 필요하다.

상세 유효성 평가:

```text
E:\memory\photomap\characteristic\2026-08-08-metric-validity-assessment.md
```

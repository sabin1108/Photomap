# Domain docs

Photomap uses a single-context layout. Before engineering work, read root
`CONTEXT.md` and relevant records under `docs/adr/` when they exist. Proceed
silently when absent. Use documented domain terms and surface ADR conflicts.
